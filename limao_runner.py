from config_setting import config
from runner import *

if __name__ == "__main__":
	# run(config['es_setting']['limao_index_name'], 
	# 	config['monitor_config']['limao_threhold_num'],
	# 	config['wechat_config']['limao_secret'],
	# 	config['wechat_config']['limao_agent_id'] )